# Input () method
# accepting user input in python
# this is a method that prompts users to empty input and returns the input as a string

input() # this can be asigned to a variable 

name = input("enter your name")
print(f"hello {name}")

# this can be aplied to age as well 
# and to define the user input we used typecast...... cause users input is a string  #

age = int(input("nter your age "))
age = age + 1
print(f"age of my age has gotten bigger {age}")  

# we could type cast it direct or do it along the road of the coe build up
# since python dosen't spcify ths variable as constant or flexible unlike javscript  #